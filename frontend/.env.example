# Frontend Environment Variables
# Copy this to .env and replace with your actual values

# After deploying backend to AWS App Runner, replace with your App Runner URL
# Format: https://<app-name>.<random-string>.<region>.awsapprunner.com
VITE_API_BASE_URL=http://localhost:8000
